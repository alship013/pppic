{
  "name": "ppic-operations-system",
  "version": "1.0.0",
  "private": true,
  "description": "Calendar-first PPIC planning, production, inventory, and control dashboard.",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && node server.js",
    "start": "npm run build && node server.js"
  },
  "devDependencies": {
    "pdf-parse": "^2.4.5",
    "pdf-to-img": "^6.1.0",
    "typescript": "^5.9.3"
  }
}
